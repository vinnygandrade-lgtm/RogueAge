Drop one cutout PNG per hunting grade, same pose as `map_bg.jpg`:

`ng.png` · `d.png` · `c.png` · `b.png` · `a.png` · `s.png`

Then:

```
npm run export:expedition
```

Hard-refresh the client. Vite does not watch `assets/`.
