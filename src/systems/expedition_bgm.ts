/**
 * Expedition BGM entry — the director lives in `game_bgm.ts`
 * (login / hub / hunt beds share one Audio element).
 */
export { syncExpeditionBgm, stopExpeditionBgm, syncGameBgm } from './game_bgm';
